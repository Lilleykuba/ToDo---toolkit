const Dashboard = ({ onClose }: { onClose: () => void }) => {
  return (
    <div className="text-4xl text-center text-primary w-full mt-4">
      Dashboard
    </div>
  );
};

export default Dashboard;
